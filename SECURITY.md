# 安全策略

## 支持版本

安全修复面向最新 1.x 稳定版本。0.1 只保留历史兼容信息，不再接收常规修复。

## 报告问题

请通过 GitHub Security Advisory 私密报告。报告应包含受影响版本、最小复现、实际影响和可行的缓解方式。不要在公开 Issue 中提交访问令牌、数据库凭据、私有快照或含敏感请求正文的日志。

## 安全范围

重点问题包括：

- 临时目录路径穿越、意外递归删除或符号链接边界；
- 深复制、深比较、套件或记录 API 可导致的无界资源消耗；
- HTTP Mock、快照或调用记录意外泄露认证信息；
- 数据库测试事务错误提交或清理失败；
- 仓储契约在非测试数据源上执行破坏性重置；
- 错误消息泄露调用方数据或凭据。

测试回调、Mock 实现、仓储适配器和数据库连接由调用方提供，均属于受信代码边界。详细模型见[docs/SECURITY_MODEL.md](docs/SECURITY_MODEL.md)。
