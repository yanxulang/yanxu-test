# 贡献指南

## 开发要求

- 使用言序 1.1.12 和仓库已提交的锁文件；
- 从公开入口添加行为，不依赖私有实现细节；
- 新增功能须覆盖正常、边界、错误和资源限制路径；
- 所有测试替身必须显式注入，不改写全局网络、时钟或数据库状态；
- 修改公共 API 时同步更新人工 API、生成参考、迁移文档和更新记录。

## 本地验证

```sh
yanxu 包 锁 --离线 .
yanxu 试 tests --并发 1 --json
yanxu 兼容 tests --json
yanxu 查 examples/服务测试.yx
yanxu 查 benchmarks/核心性能.yx
yanxu 编 . --release -o /tmp/yanxu-test.yxb
```

示例和基准是独立消费者包，修改根公共 API 后还须分别验证其清单、锁、静态检查、运行和 Release 构建。

## 提交范围

每个提交应是可独立验证的完整变化。不要把功能、文档、CI 和发布元数据塞进同一提交，也不要提交构建目录、缓存、临时快照或凭据。
