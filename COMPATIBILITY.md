# 兼容性

## 版本矩阵

| 言试 | 最低言序 | 言据 | 言时 | 言访 | 言库 |
| --- | --- | --- | --- | --- | --- |
| 1.0.x | 1.1.12 | 1.2.x | 1.0.x | 1.0.x | 1.0.x |
| 0.1.x | 1.1.6 | 无直接依赖 | main 开发线 | 无 | 无 |

1.0 的清单固定稳定标签，并由锁文件继续固定精确提交。消费者应提交自己的锁文件，不应把临时分支作为发布依赖。

## 执行平台

言试为纯言序包，不包含原生扩展或平台制品。Linux、macOS 与 Windows 使用相同公共 API；文件路径仍遵循宿主平台规则，但`临时目录.合并()`统一要求正斜线相对路径并拒绝`..`。

## 公共边界

- 1.0 保留 0.1 的断言、Mock、Spy、快照、`HTTPMock.传输()`和通用`测试事务()`；
- 新代码可使用`HTTPMock.言访传输()`、`数据库测试事务()`和仓储契约；
- 1.0 的错误代码是兼容边界，中文消息不是；
- `尝试运行()`的结构化字段是机器消费边界；
- 快照 JSON 文件只保证 1.x 内兼容，不承诺与其他快照框架互通。

## 权限边界

依赖权限不传递。使用快照、临时目录或系统环境读取的顶层项目必须自行声明相应文件或环境权限。HTTP Mock、函数仓储适配器和数据库测试事务不会扩大调用方已有权限。
