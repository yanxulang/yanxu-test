引「包:言试」为 言试；

定 时钟 为 言试.虚拟时钟（0）；

定 HTTP 为 言试.HTTPMock（）.响应JSON（「GET」，「https://api.example.test/health」，200，{「ok」：真}）；

定 调用 为 言试.Mock（时钟）.返回（「完成」）；

定 结果 为 调用.调用（【「任务-1」】）；

言试.相等（结果，「完成」）；

言试.相等（调用.调用次数（），1）；

言试.相等（HTTP.请求（「GET」，「https://api.example.test/health」，{}，空）【「状态」】，200）；
