引「标准:JSON」为 JSON；

引「标准:数学」为 数学；

引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:文件」为 文件；

引「标准:路径」为 路径；

引「标准:环境」为 环境；

引「标准:标识」为 标识；

引「标准:字节」为 字节；

引「包:言时」为 言时；

引「包:言访」为 言访；

引「包:言库」为 言库；

公 定 版本：文 为 「1.0.0」；

公 定 默认最大比较深度：数 为 64；

公 定 默认最大比较节点：数 为 100000；

公 定 默认最大套件测试：数 为 4096；

公 定 默认最大套件夹具：数 为 256；

公 定 默认最大套件钩子：数 为 256；

定 最大允许比较深度：数 为 256；

定 最大允许比较节点：数 为 1000000；

令 临时序号：数 为 0；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANSHI_TEST_」 加 代码） 加 「：」） 加 消息）；
终

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 找片（原文：文，片：文）：数? 则
    若 （长度（片） 等于 0） 则
        归 0；
    终
    令 位：数 为 0；
    当 （（位 加 长度（片）） 不大于 长度（原文）） 则
        若 （原文【位：（位 加 长度（片））】 等于 片） 则
            归 位；
        终
        置 位 为 （位 加 1）；
    终
    归 空；
终

公 法 错误详情（所误：误）：典 则
    定 原消息：文 为 所误.消息；
    定 分隔：数? 为 找片（原消息，「：」）；
    令 代码：文 为 「YANSHI_TEST_RUNTIME」；
    令 消息：文 为 原消息；
    若 （（分隔 是 数） 且 文字.始于（原消息，「YANSHI_TEST_」）） 则
        置 代码 为 原消息【0：分隔】；
        置 消息 为 原消息【（分隔 加 1）：】；
    终
    归 {「代码」：代码，「消息」：消息，「源代码」：所误.代码，「类别」：所误.类别，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

法 整数限制（值：数，名称：文，最大：数）：数 则
    若 （（（值 小于 1） 或 （数学.下取整（值） 不等于 值）） 或 （值 大于 最大）） 则
        报错（「LIMIT」，（（（名称 加 「须为 1 至 」） 加 整数文（最大）） 加 「 的整数」））；
    终
    归 值；
终

公 法 默认限制（）：典 则
    归 {「最大比较深度」：默认最大比较深度，「最大比较节点」：默认最大比较节点，「最大套件测试」：默认最大套件测试，「最大套件夹具」：默认最大套件夹具，「最大套件钩子」：默认最大套件钩子}；
终

法 检查名称（名称：文，种类：文）：文 则
    若 （（长度（名称） 小于 1） 或 （长度（名称） 大于 256）） 则
        报错（「NAME」，（种类 加 「名称须为一至 256 字符」））；
    终
    归 名称；
终

法 定义含名（定义列：列<典>，名称：文）：理 则
    逐 定义：典 于 定义列 则
        若 （定义【「名称」】 等于 名称） 则
            归 真；
        终
    终
    归 假；
终

公 法 复制值受限（来源，最大深度：数，最大节点：数） 则
    定 深度上限：数 为 整数限制（最大深度，「最大比较深度」，最大允许比较深度）；
    定 节点上限：数 为 整数限制（最大节点，「最大比较节点」，最大允许比较节点）；
    令 已节点：数 为 0；
    法 复制内（当前，深度：数） 则
        若 （深度 大于 深度上限） 则
            报错（「COPY_LIMIT」，「复制值超过最大深度」）；
        终
        置 已节点 为 （已节点 加 1）；
        若 （已节点 大于 节点上限） 则
            报错（「COPY_LIMIT」，「复制值超过最大节点数」）；
        终
        若 （当前 是 列） 则
            令 结果列：列 为 【】；
            逐 项 于 当前 则
                追加（结果列，复制内（项，（深度 加 1）））；
            终
            归 结果列；
        终
        若 （当前 是 典） 则
            令 结果典：典 为 {}；
            逐 键 于 当前 则
                置 结果典【键】 为 复制内（当前【键】，（深度 加 1））；
            终
            归 结果典；
        终
        若 （当前 是 字节串） 则
            归 字节.从数列（字节.转数列（当前））；
        终
        归 当前；
    终
    归 复制内（来源，0）；
终

公 法 复制值（来源） 则
    归 复制值受限（来源，默认最大比较深度，默认最大比较节点）；
终

法 复制列（来源：列）：列 则
    归 复制值（来源）；
终

法 复制典（来源：典）：典 则
    归 复制值（来源）；
终

法 去键（来源：典，目标）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        若 （键 不等于 目标） 则
            置 结果【键】 为 复制值（来源【键】）；
        终
    终
    归 结果；
终

法 整数文（值：数）：文 则
    归 文字.替换（JSON.序列化（值），「.0」，「」）；
终

法 文含（原文：文，片段：文）：理 则
    若 （长度（片段） 等于 0） 则
        归 真；
    终
    若 （长度（片段） 大于 长度（原文）） 则
        归 假；
    终
    令 位：数 为 0；
    当 （（位 加 长度（片段）） 不大于 长度（原文）） 则
        若 （原文【位：（位 加 长度（片段））】 等于 片段） 则
            归 真；
        终
        置 位 为 （位 加 1）；
    终
    归 假；
终

法 展示（值）：文 则
    试 则
        归 JSON.序列化（值）；
    救 所误 则
        归 （（「<」 加 类型（值）） 加 「>」）；
    终
终

法 错误典（所误：误）：典 则
    归 错误详情（所误）；
终

公 法 断言（条件：理，消息：文）：空 则
    若 非 条件 则
        报错（「ASSERT」，消息）；
    终
    归 空；
终

公 法 失败（消息：文）：空 则
    报错（「FAIL」，消息）；
终

公 法 相等（实际，期望）：空 则
    若 （实际 不等于 期望） 则
        报错（「EQUAL」，（（（「期望 」 加 展示（期望）） 加 「，实际 」） 加 展示（实际）））；
    终
    归 空；
终

公 法 不相等（实际，期望）：空 则
    若 （实际 等于 期望） 则
        报错（「NOT_EQUAL」，（「两个值不应相等：」 加 展示（实际）））；
    终
    归 空；
终

法 是空值（值）：理 则
    若 （值 等于 空） 则
        归 真；
    终
    若 （值 是 文） 则
        归 （长度（值） 等于 0）；
    终
    若 （值 是 列） 则
        归 （长度（值） 等于 0）；
    终
    若 （值 是 典） 则
        归 （长度（键列（值）） 等于 0）；
    终
    若 （值 是 字节串） 则
        归 （字节.长度（值） 等于 0）；
    终
    归 假；
终

公 法 应为空（值）：空 则
    若 非 是空值（值） 则
        报错（「EMPTY」，（「期望空值，实际 」 加 展示（值）））；
    终
    归 空；
终

公 法 应不为空（值）：空 则
    若 是空值（值） 则
        报错（「NOT_EMPTY」，「期望非空值」）；
    终
    归 空；
终

法 加差异（结果：列，路径值：文，代码：文，期望，实际）：空 则
    追加（结果，{「路径」：路径值，「代码」：代码，「期望」：期望，「实际」：实际}）；
    归 空；
终

公 法 深差异受限（实际，期望，最大深度：数，最大节点：数）：列 则
    定 深度上限：数 为 整数限制（最大深度，「最大比较深度」，最大允许比较深度）；
    定 节点上限：数 为 整数限制（最大节点，「最大比较节点」，最大允许比较节点）；
    令 结果：列 为 【】；
    令 已节点：数 为 0；
    法 比较（甲，乙，所径：文，深度：数）：空 则
        若 （深度 大于 深度上限） 则
            报错（「COMPARE_LIMIT」，「深比较超过最大深度」）；
        终
        置 已节点 为 （已节点 加 1）；
        若 （已节点 大于 节点上限） 则
            报错（「COMPARE_LIMIT」，「深比较超过最大节点数」）；
        终
        若 （类型（甲） 不等于 类型（乙）） 则
            加差异（结果，所径，「TYPE」，类型（乙），类型（甲））；
            归 空；
        终
        若 （甲 是 列） 则
            若 （长度（甲） 不等于 长度（乙）） 则
                加差异（结果，所径，「LENGTH」，长度（乙），长度（甲））；
            终
            令 位：数 为 0；
            当 （（位 小于 长度（甲）） 且 （位 小于 长度（乙））） 则
                比较（甲【位】，乙【位】，（（（所径 加 「[」） 加 整数文（位）） 加 「]」），（深度 加 1））；
                置 位 为 （位 加 1）；
            终
            归 空；
        终
        若 （甲 是 典） 则
            逐 键 于 乙 则
                若 非 有所键（甲，键） 则
                    加差异（结果，（（所径 加 「.」） 加 展示（键）），「MISSING_KEY」，乙【键】，空）；
                否则
                    比较（甲【键】，乙【键】，（（所径 加 「.」） 加 展示（键）），（深度 加 1））；
                终
            终
            逐 键 于 甲 则
                若 非 有所键（乙，键） 则
                    加差异（结果，（（所径 加 「.」） 加 展示（键）），「EXTRA_KEY」，空，甲【键】）；
                终
            终
            归 空；
        终
        若 （甲 不等于 乙） 则
            加差异（结果，所径，「VALUE」，乙，甲）；
        终
        归 空；
    终
    比较（实际，期望，「$」，0）；
    归 结果；
终

公 法 深差异（实际，期望）：列 则
    归 深差异受限（实际，期望，默认最大比较深度，默认最大比较节点）；
终

公 法 深相等（实际，期望）：空 则
    定 差异：列 为 深差异（实际，期望）；
    若 （长度（差异） 大于 0） 则
        报错（「DEEP_EQUAL」，JSON.序列化（差异））；
    终
    归 空；
终

公 法 近似相等（实际：数，期望：数，容差：数）：空 则
    若 （容差 小于 0） 则
        报错（「TOLERANCE」，「容差不可为负数」）；
    终
    若 （数学.绝对值（（实际 减 期望）） 大于 容差） 则
        报错（「APPROXIMATE」，（（（（（「期望约等于 」 加 展示（期望）） 加 「，实际 」） 加 展示（实际）） 加 「，容差 」） 加 展示（容差）））；
    终
    归 空；
终

公 法 应含（容器，期望）：空 则
    令 已含：理 为 假；
    若 （容器 是 文） 则
        若 非 （期望 是 文） 则
            报错（「CONTAINS_TYPE」，「文字包含断言的期望值也须为文字」）；
        终
        置 已含 为 文含（容器，期望）；
    否则
        若 （容器 是 列） 则
            置 已含 为 包含（容器，期望）；
        否则
            若 （容器 是 典） 则
                置 已含 为 有所键（容器，期望）；
            否则
                报错（「CONTAINS_TYPE」，「包含断言只支持文、列或典」）；
            终
        终
    终
    若 非 已含 则
        报错（「CONTAINS」，（「容器不包含 」 加 展示（期望）））；
    终
    归 空；
终

公 法 应匹配（原文：文，模式：文）：空 则
    若 非 正则.匹配（模式，原文） 则
        报错（「MATCH」，（（（「文字不匹配模式 」 加 模式） 加 「：」） 加 原文））；
    终
    归 空；
终

公 法 类型断言（值，期望类型：文）：空 则
    定 实际类型：文 为 类型（值）；
    若 （实际类型 不等于 期望类型） 则
        报错（「TYPE」，（（（「期望类型 」 加 期望类型） 加 「，实际 」） 加 实际类型））；
    终
    归 空；
终

公 法 符合（值，判定：法，名称：文）：空 则
    若 非 判定（值） 则
        报错（「PREDICATE」，（「值不符合类型/条件 」 加 名称））；
    终
    归 空；
终

公 法 应当抛错（操作：法）：误 则
    试 则
        操作（）；
    救 所误 则
        归 所误；
    终
    报错（「THROWS」，「期望操作抛错，但操作成功」）；
终

公 法 应当抛错匹配（操作：法，模式：文）：误 则
    定 所误：误 为 应当抛错（操作）；
    若 非 正则.匹配（模式，所误.消息） 则
        报错（「THROWS_MATCH」，（（（「错误消息不匹配 」 加 模式） 加 「：」） 加 所误.消息））；
    终
    归 所误；
终

公 法 应当抛错代码（操作：法，代码：文）：误 则
    定 所误：误 为 应当抛错（操作）；
    定 详情：典 为 错误详情（所误）；
    若 （详情【「代码」】 不等于 代码） 则
        报错（「THROWS_CODE」，（（（「期望错误代码 」 加 代码） 加 「，实际 」） 加 详情【「代码」】））；
    终
    归 所误；
终

公 法 表格测试（名称：文，各行：列，测试法：法）：典 则
    令 通过数：数 为 0；
    令 结果：列 为 【】；
    令 位：数 为 0；
    逐 行 于 各行 则
        令 行名：文 为 （（名称 加 「 #」） 加 整数文（（位 加 1）））；
        若 （行 是 典） 则
            若 有所键（行，「名称」） 则
                置 行名 为 行【「名称」】；
            终
        终
        试 则
            测试法（行）；
            追加（结果，{「名称」：行名，「成功」：真}）；
            置 通过数 为 （通过数 加 1）；
        救 所误 则
            追加（结果，{「名称」：行名，「成功」：假，「错误」：错误典（所误）}）；
            报错（「TABLE」，（（行名 加 「：」） 加 所误.消息））；
        终
        置 位 为 （位 加 1）；
    终
    归 {「名称」：名称，「总数」：长度（各行），「通过」：通过数，「结果」：结果}；
终

公 法 参数化测试（名称：文，各参数：列<列>，测试法：法）：典 则
    令 各行：列 为 【】；
    令 位：数 为 0；
    逐 参数：列 于 各参数 则
        追加（各行，{「名称」：（（名称 加 「 #」） 加 整数文（（位 加 1））），「参数」：参数}）；
        置 位 为 （位 加 1）；
    终
    法 执行（行：典）：空 则
        测试法（行【「参数」】）；
        归 空；
    终
    归 表格测试（名称，各行，执行）；
终

公 类 测试夹具 则
    公 只 域 名称：文；
    私 域 工厂：法；
    私 域 清理法：法；
    私 域 已创建：理；
    私 域 值：任意；
    公 法 初始化（名称：文，工厂：法，清理法：法） 则
        置 此.名称 为 名称；
        置 此.工厂 为 工厂；
        置 此.清理法 为 清理法；
        置 此.已创建 为 假；
        置 此.值 为 空；
    终
    公 法 取得（） 则
        若 非 此.已创建 则
            置 此.值 为 此.工厂（）；
            置 此.已创建 为 真；
        终
        归 此.值；
    终
    公 法 清理（）：空 则
        若 此.已创建 则
            定 待清理 为 此.值；
            置 此.值 为 空；
            置 此.已创建 为 假；
            此.清理法（待清理）；
        终
        归 空；
    终
终

公 类 测试上下文 则
    私 域 夹具典：典；
    私 域 夹具名列：列<文>；
    公 法 初始化（定义列：列） 则
        若 （长度（定义列） 大于 默认最大套件夹具） 则
            报错（「SUITE_LIMIT」，「夹具数量超过上限」）；
        终
        置 此.夹具典 为 {}；
        置 此.夹具名列 为 【】；
        逐 定义：典 于 定义列 则
            定 名称：文 为 检查名称（定义【「名称」】，「夹具」）；
            若 有所键（此.夹具典，名称） 则
                报错（「FIXTURE_DUPLICATE」，（「夹具名称重复：」 加 名称））；
            终
            若 （非 （定义【「工厂」】 是 法） 或 非 （定义【「清理」】 是 法）） 则
                报错（「FIXTURE_TYPE」，（「夹具工厂和清理法必须可调用：」 加 名称））；
            终
            定 所具 为 测试夹具（名称，定义【「工厂」】，定义【「清理」】）；
            置 此.夹具典【名称】 为 所具；
            追加（此.夹具名列，名称）；
        终
    终
    公 法 夹具（名称：文） 则
        若 非 有所键（此.夹具典，名称） 则
            报错（「FIXTURE_UNKNOWN」，（「未知夹具 」 加 名称））；
        终
        归 此.夹具典【名称】.取得（）；
    终
    公 法 清理错误列（）：列<误> 则
        令 错误列：列<误> 为 【】；
        令 位：数 为 长度（此.夹具名列）；
        当 （位 大于 0） 则
            置 位 为 （位 减 1）；
            试 则
                此.夹具典【此.夹具名列【位】】.清理（）；
            救 所误 则
                追加（错误列，所误）；
            终
        终
        归 错误列；
    终
    公 法 清理（）：空 则
        定 错误列：列<误> 为 此.清理错误列（）；
        若 （长度（错误列） 大于 0） 则
            抛 错误列【0】；
        终
        归 空；
    终
终

公 类 测试套件 则
    公 只 域 名称：文；
    私 域 前全部：列<法>；
    私 域 后全部：列<法>；
    私 域 前每项：列<法>；
    私 域 后每项：列<法>；
    私 域 测试列：列<典>；
    私 域 夹具定义：列<典>；
    公 法 初始化（名称：文） 则
        置 此.名称 为 检查名称（名称，「套件」）；
        置 此.前全部 为 【】；
        置 此.后全部 为 【】；
        置 此.前每项 为 【】；
        置 此.后每项 为 【】；
        置 此.测试列 为 【】；
        置 此.夹具定义 为 【】；
    终
    公 法 测试前（钩子：法）：测试套件 则
        若 （长度（此.前全部） 不小于 默认最大套件钩子） 则
            报错（「SUITE_LIMIT」，「测试前钩子数量超过上限」）；
        终
        追加（此.前全部，钩子）；
        归 此；
    终
    公 法 测试后（钩子：法）：测试套件 则
        若 （长度（此.后全部） 不小于 默认最大套件钩子） 则
            报错（「SUITE_LIMIT」，「测试后钩子数量超过上限」）；
        终
        追加（此.后全部，钩子）；
        归 此；
    终
    公 法 每项前（钩子：法）：测试套件 则
        若 （长度（此.前每项） 不小于 默认最大套件钩子） 则
            报错（「SUITE_LIMIT」，「每项前钩子数量超过上限」）；
        终
        追加（此.前每项，钩子）；
        归 此；
    终
    公 法 每项后（钩子：法）：测试套件 则
        若 （长度（此.后每项） 不小于 默认最大套件钩子） 则
            报错（「SUITE_LIMIT」，「每项后钩子数量超过上限」）；
        终
        追加（此.后每项，钩子）；
        归 此；
    终
    公 法 夹具（名称：文，工厂：法，清理法：法）：测试套件 则
        定 已验名：文 为 检查名称（名称，「夹具」）；
        若 （长度（此.夹具定义） 不小于 默认最大套件夹具） 则
            报错（「SUITE_LIMIT」，「夹具数量超过上限」）；
        终
        若 定义含名（此.夹具定义，已验名） 则
            报错（「FIXTURE_DUPLICATE」，（「夹具名称重复：」 加 已验名））；
        终
        追加（此.夹具定义，{「名称」：已验名，「工厂」：工厂，「清理」：清理法}）；
        归 此；
    终
    公 法 测试（名称：文，测试法：法）：测试套件 则
        定 已验名：文 为 检查名称（名称，「测试」）；
        若 （长度（此.测试列） 不小于 默认最大套件测试） 则
            报错（「SUITE_LIMIT」，「测试数量超过上限」）；
        终
        若 定义含名（此.测试列，已验名） 则
            报错（「TEST_DUPLICATE」，（「测试名称重复：」 加 已验名））；
        终
        追加（此.测试列，{「名称」：已验名，「测试」：测试法}）；
        归 此；
    终
    公 法 尝试运行（）：典 则
        令 结果：列 为 【】；
        令 失败数：数 为 0；
        令 通过数：数 为 0；
        令 跳过数：数 为 0；
        令 生命周期错误：列 为 【】；
        令 前置成功：理 为 真；
        法 加错误（错误列：列，阶段：文，所误：误）：空 则
            定 详情：典 为 错误典（所误）；
            置 详情【「阶段」】 为 阶段；
            追加（错误列，详情）；
            归 空；
        终
        逐 钩子：法 于 此.前全部 则
            试 则
                钩子（）；
            救 所误 则
                加错误（生命周期错误，「测试前」，所误）；
                置 前置成功 为 假；
            终
        终
        逐 所测：典 于 此.测试列 则
            若 非 前置成功 则
                追加（结果，{「名称」：所测【「名称」】，「成功」：假，「跳过」：真，「原因」：「测试前钩子失败」}）；
                置 跳过数 为 （跳过数 加 1）；
            否则
                定 上下文：测试上下文 为 测试上下文（此.夹具定义）；
                令 错误列：列 为 【】；
                令 每项前成功：理 为 真；
                逐 钩子：法 于 此.前每项 则
                    若 每项前成功 则
                        试 则
                            钩子（上下文）；
                        救 所误 则
                            加错误（错误列，「每项前」，所误）；
                            置 每项前成功 为 假；
                        终
                    终
                终
                若 每项前成功 则
                    试 则
                        所测【「测试」】（上下文）；
                    救 所误 则
                        加错误（错误列，「测试」，所误）；
                    终
                终
                逐 钩子：法 于 此.后每项 则
                    试 则
                        钩子（上下文）；
                    救 所误 则
                        加错误（错误列，「每项后」，所误）；
                    终
                终
                逐 所误：误 于 上下文.清理错误列（） 则
                    加错误（错误列，「夹具清理」，所误）；
                终
                若 （长度（错误列） 大于 0） 则
                    追加（结果，{「名称」：所测【「名称」】，「成功」：假，「跳过」：假，「错误」：错误列【0】，「错误列」：错误列}）；
                    置 失败数 为 （失败数 加 1）；
                否则
                    追加（结果，{「名称」：所测【「名称」】，「成功」：真，「跳过」：假}）；
                    置 通过数 为 （通过数 加 1）；
                终
            终
        终
        逐 钩子：法 于 此.后全部 则
            试 则
                钩子（）；
            救 所误 则
                加错误（生命周期错误，「测试后」，所误）；
            终
        终
        归 {「名称」：此.名称，「总数」：长度（此.测试列），「通过」：通过数，「失败」：失败数，「跳过」：跳过数，「生命周期失败」：长度（生命周期错误），「成功」：（（（失败数 等于 0） 且 （跳过数 等于 0）） 且 （长度（生命周期错误） 等于 0）），「结果」：结果，「生命周期错误」：生命周期错误}；
    终
    公 法 运行（）：典 则
        定 运行结果：典 为 此.尝试运行（）；
        若 非 运行结果【「成功」】 则
            报错（「SUITE」，（（（（（此.名称 加 「 失败数：」） 加 整数文（运行结果【「失败」】）） 加 「，跳过数：」） 加 整数文（运行结果【「跳过」】）） 加 （「，生命周期失败：」 加 整数文（运行结果【「生命周期失败」】））））；
        终
        归 运行结果；
    终
终

公 法 套件（名称：文）：测试套件 则
    归 测试套件（名称）；
终

法 安全相对路径（相对：文）：文 则
    若 （（长度（相对） 等于 0） 或 路径.是否绝对（相对）） 则
        报错（「TEMP_PATH」，「临时目录子路径必须是非空相对路径」）；
    终
    若 文含（相对，「\\」） 则
        报错（「TEMP_PATH」，「临时目录子路径须使用正斜线」）；
    终
    逐 段：文 于 文字.分割（相对，「/」） 则
        若 （（段 等于 「」） 或 （段 等于 「..」）） 则
            报错（「TEMP_PATH」，「临时目录子路径不可越过根目录」）；
        终
    终
    定 已规范：文 为 路径.规范化（相对）；
    若 （（已规范 等于 「.」） 或 （已规范 等于 「..」）） 则
        报错（「TEMP_PATH」，「临时目录子路径不可指向当前或上级目录」）；
    终
    归 已规范；
终

公 类 临时目录 则
    公 只 域 路径值：文；
    私 域 已清理：理；
    公 法 初始化（基础目录：文） 则
        若 （长度（基础目录） 等于 0） 则
            报错（「TEMP_ROOT」，「临时目录基础路径不可为空」）；
        终
        若 非 文件.存在（基础目录） 则
            文件.创建目录（基础目录）；
        否则
            若 （文件.状态（基础目录）【「种类」】 不等于 「目录」） 则
                报错（「TEMP_ROOT」，「临时目录基础路径必须是目录」）；
            终
        终
        置 临时序号 为 （临时序号 加 1）；
        令 种子：文 为 （（整数文（言时.系统时钟（）.毫秒（）） 加 「-」） 加 整数文（临时序号））；
        令 名称：文 为 （「yanxu-test-」 加 标识.稳定短码（种子，12））；
        令 候选：文 为 路径.合并（基础目录，名称）；
        当 文件.存在（候选） 则
            置 临时序号 为 （临时序号 加 1）；
            置 种子 为 （（整数文（言时.系统时钟（）.毫秒（）） 加 「-」） 加 整数文（临时序号））；
            置 名称 为 （「yanxu-test-」 加 标识.稳定短码（种子，12））；
            置 候选 为 路径.合并（基础目录，名称）；
        终
        置 此.路径值 为 候选；
        文件.创建目录（此.路径值）；
        置 此.已清理 为 假；
    终
    公 法 路径（）：文 则
        归 此.路径值；
    终
    公 法 合并（相对：文）：文 则
        归 路径.合并（此.路径值，安全相对路径（相对））；
    终
    公 法 是否清理（）：理 则
        归 此.已清理；
    终
    公 法 清理（）：空 则
        若 非 此.已清理 则
            若 文件.存在（此.路径值） 则
                文件.删除（此.路径值，真）；
            终
            置 此.已清理 为 真；
        终
        归 空；
    终
终

公 法 建临时目录（基础目录：文）：临时目录 则
    归 临时目录（基础目录）；
终

公 法 默认临时目录（）：临时目录 则
    归 临时目录（「.yanxu-test-tmp」）；
终

公 类 环境隔离 则
    私 域 基础：典；
    私 域 覆盖：典；
    私 域 已删：列<文>；
    公 法 初始化（基础：典） 则
        置 此.基础 为 复制典（基础）；
        置 此.覆盖 为 {}；
        置 此.已删 为 【】；
    终
    公 法 读取（名称：文）：文? 则
        若 包含（此.已删，名称） 则
            归 空；
        终
        若 有所键（此.覆盖，名称） 则
            归 此.覆盖【名称】；
        终
        若 有所键（此.基础，名称） 则
            归 此.基础【名称】；
        终
        归 空；
    终
    公 法 存在（名称：文）：理 则
        归 （此.读取（名称） 不等于 空）；
    终
    公 法 设置（名称：文，值：文）：环境隔离 则
        置 此.覆盖【名称】 为 值；
        若 包含（此.已删，名称） 则
            令 新列：列<文> 为 【】；
            逐 项：文 于 此.已删 则
                若 （项 不等于 名称） 则
                    追加（新列，项）；
                终
            终
            置 此.已删 为 新列；
        终
        归 此；
    终
    公 法 删除（名称：文）：环境隔离 则
        若 非 包含（此.已删，名称） 则
            追加（此.已删，名称）；
        终
        若 有所键（此.覆盖，名称） 则
            置 此.覆盖 为 去键（此.覆盖，名称）；
        终
        归 此；
    终
    公 法 转典（）：典 则
        令 结果：典 为 复制典（此.基础）；
        逐 名称：文 于 此.已删 则
            若 有所键（结果，名称） 则
                置 结果 为 去键（结果，名称）；
            终
        终
        逐 名称 于 此.覆盖 则
            置 结果【名称】 为 此.覆盖【名称】；
        终
        归 结果；
    终
    公 法 恢复（）：环境隔离 则
        置 此.覆盖 为 {}；
        置 此.已删 为 【】；
        归 此；
    终
终

公 法 隔离环境（基础：典）：环境隔离 则
    归 环境隔离（基础）；
终

公 法 隔离系统环境（名称列：列<文>）：环境隔离 则
    令 基础：典 为 {}；
    逐 名称：文 于 名称列 则
        若 环境.存在（名称） 则
            置 基础【名称】 为 环境.读取（名称）；
        终
    终
    归 环境隔离（基础）；
终

公 类 模拟函数 则
    私 域 实现法：法?；
    私 域 默认值：任意；
    私 域 返回列：列；
    私 域 抛错列：列<文>；
    私 域 调用列：列<典>；
    私 域 时钟：任意；
    公 法 初始化（时钟） 则
        置 此.实现法 为 空；
        置 此.默认值 为 空；
        置 此.返回列 为 【】；
        置 此.抛错列 为 【】；
        置 此.调用列 为 【】；
        置 此.时钟 为 时钟；
    终
    公 法 返回（值）：模拟函数 则
        置 此.默认值 为 值；
        归 此；
    终
    公 法 依次返回（各值：列）：模拟函数 则
        置 此.返回列 为 复制列（各值）；
        归 此；
    终
    公 法 依次抛错（各消息：列<文>）：模拟函数 则
        置 此.抛错列 为 复制列（各消息）；
        归 此；
    终
    公 法 实现（所法：法）：模拟函数 则
        置 此.实现法 为 所法；
        归 此；
    终
    公 法 调用（参数：列） 则
        定 序号：数 为 （长度（此.调用列） 加 1）；
        定 记录：典 为 {「序号」：序号，「参数」：复制列（参数），「时间戳」：此.时钟.毫秒（），「成功」：假}；
        若 （长度（此.抛错列） 大于 0） 则
            定 消息：文 为 此.抛错列【0】；
            置 此.抛错列 为 此.抛错列【1：】；
            置 记录【「错误」】 为 消息；
            追加（此.调用列，记录）；
            抛 消息；
        终
        试 则
            令 结果 为 此.默认值；
            若 （长度（此.返回列） 大于 0） 则
                置 结果 为 此.返回列【0】；
                置 此.返回列 为 此.返回列【1：】；
            否则
                若 （此.实现法 是 法） 则
                    置 结果 为 此.实现法（参数）；
                终
            终
            置 记录【「成功」】 为 真；
            置 记录【「结果」】 为 结果；
            追加（此.调用列，记录）；
            归 结果；
        救 所误 则
            置 记录【「错误」】 为 错误典（所误）；
            追加（此.调用列，记录）；
            抛 所误；
        终
    终
    公 法 函数（）：法 则
        归 此.调用；
    终
    公 法 调用次数（）：数 则
        归 长度（此.调用列）；
    终
    公 法 各调用（）：列<典> 则
        归 复制列（此.调用列）；
    终
    公 法 第次（序号：数）：典 则
        若 （（序号 小于 1） 或 （序号 大于 长度（此.调用列））） 则
            报错（「MOCK_CALL」，「调用序号越界」）；
        终
        归 此.调用列【（序号 减 1）】；
    终
    公 法 被调用于（参数：列）：理 则
        逐 记录：典 于 此.调用列 则
            若 （长度（深差异（记录【「参数」】，参数）） 等于 0） 则
                归 真；
            终
        终
        归 假；
    终
    公 法 清空（）：模拟函数 则
        置 此.调用列 为 【】；
        归 此；
    终
终

公 法 Mock（时钟）：模拟函数 则
    归 模拟函数（时钟）；
终

公 法 默认Mock（）：模拟函数 则
    归 模拟函数（言时.系统时钟（））；
终

公 类 Spy函数 则
    私 域 原函数：法；
    私 域 模拟：模拟函数；
    公 法 初始化（原函数：法，时钟） 则
        置 此.原函数 为 原函数；
        置 此.模拟 为 模拟函数（时钟）.实现（原函数）；
    终
    公 法 调用（参数：列） 则
        归 此.模拟.调用（参数）；
    终
    公 法 函数（）：法 则
        归 此.调用；
    终
    公 法 调用次数（）：数 则
        归 此.模拟.调用次数（）；
    终
    公 法 各调用（）：列<典> 则
        归 此.模拟.各调用（）；
    终
    公 法 第次（序号：数）：典 则
        归 此.模拟.第次（序号）；
    终
    公 法 被调用于（参数：列）：理 则
        归 此.模拟.被调用于（参数）；
    终
终

公 法 Spy（原函数：法，时钟）：Spy函数 则
    归 Spy函数（原函数，时钟）；
终

公 法 默认Spy（原函数：法）：Spy函数 则
    归 Spy函数（原函数，言时.系统时钟（））；
终

公 法 虚拟时钟（起始毫秒：数） 则
    归 言时.虚拟时钟（起始毫秒）；
终

公 类 快照库 则
    公 只 域 文件路径：文；
    私 域 更新：理；
    私 域 各快照：典；
    私 域 已改：理；
    公 法 初始化（文件路径：文，更新：理） 则
        置 此.文件路径 为 文件路径；
        置 此.更新 为 更新；
        置 此.各快照 为 {}；
        置 此.已改 为 假；
        若 文件.存在（文件路径） 则
            定 已读 为 JSON.解析（文件.读取（文件路径））；
            若 非 （已读 是 典） 则
                报错（「SNAPSHOT_FILE」，「快照文件根值必须是对象」）；
            终
            置 此.各快照 为 已读；
        终
    终
    公 法 匹配（名称：文，值）：空 则
        若 非 有所键（此.各快照，名称） 则
            若 非 此.更新 则
                报错（「SNAPSHOT_MISSING」，（「缺少快照 」 加 名称））；
            终
            置 此.各快照【名称】 为 值；
            置 此.已改 为 真；
            此.保存（）；
            归 空；
        终
        定 差异：列 为 深差异（值，此.各快照【名称】）；
        若 （长度（差异） 大于 0） 则
            若 此.更新 则
                置 此.各快照【名称】 为 值；
                置 此.已改 为 真；
                此.保存（）；
            否则
                报错（「SNAPSHOT_MISMATCH」，JSON.序列化（{「名称」：名称，「差异」：差异}））；
            终
        终
        归 空；
    终
    公 法 保存（）：空 则
        若 此.已改 则
            文件.写入（此.文件路径，JSON.序列化（此.各快照））；
            置 此.已改 为 假；
        终
        归 空；
    终
    公 法 名称列（）：列 则
        归 键列（此.各快照）；
    终
终

公 类 HTTPMock 则
    私 域 路由列：列<典>；
    私 域 请求列：列<典>；
    公 法 初始化（） 则
        置 此.路由列 为 【】；
        置 此.请求列 为 【】；
    终
    私 法 加路由（方法：文，匹配种类：文，地址：文，状态：数，首部：典，正文，剩余：数）：HTTPMock 则
        追加（此.路由列，{「方法」：文字.大写（方法），「匹配」：匹配种类，「地址」：地址，「状态」：状态，「首部」：复制典（首部），「正文」：正文，「剩余」：剩余}）；
        归 此；
    终
    公 法 响应（方法：文，地址：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「精确」，地址，状态，首部，正文，减 1）；
    终
    公 法 响应匹配（方法：文，模式：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「正则」，模式，状态，首部，正文，减 1）；
    终
    公 法 一次响应（方法：文，地址：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「精确」，地址，状态，首部，正文，1）；
    终
    公 法 响应JSON（方法：文，地址：文，状态：数，值）：HTTPMock 则
        归 此.响应（方法，地址，状态，{「content-type」：「application/json」}，JSON.序列化（值））；
    终
    私 法 匹配请求（请求：典）：典 则
        定 已请求：典 为 复制典（请求）；
        置 已请求【「方法」】 为 文字.大写（已请求【「方法」】）；
        追加（此.请求列，已请求）；
        逐 路由：典 于 此.路由列 则
            令 地址合：理 为 假；
            若 （路由【「匹配」】 等于 「精确」） 则
                置 地址合 为 （已请求【「地址」】 等于 路由【「地址」】）；
            否则
                置 地址合 为 正则.匹配（路由【「地址」】，已请求【「地址」】）；
            终
            若 （（地址合 且 （已请求【「方法」】 等于 路由【「方法」】）） 且 （路由【「剩余」】 不等于 0）） 则
                若 （路由【「剩余」】 大于 0） 则
                    置 路由【「剩余」】 为 （路由【「剩余」】 减 1）；
                终
                归 {「状态」：路由【「状态」】，「地址」：已请求【「地址」】，「首部」：复制典（路由【「首部」】），「正文」：复制值（路由【「正文」】）}；
            终
        终
        报错（「HTTP_UNMATCHED」，（（已请求【「方法」】 加 「 」） 加 已请求【「地址」】））；
    终
    公 法 请求（方法：文，地址：文，首部：典，正文） 则
        归 此.匹配请求（{「方法」：文字.大写（方法），「地址」：地址，「首部」：复制典（首部），「正文」：正文}）；
    终
    私 法 发送言访（快照：典）：典 则
        定 响应：典 为 此.匹配请求（快照）；
        若 （响应【「正文」】 是 文） 则
            置 响应【「正文」】 为 字节.从文字（响应【「正文」】）；
        否则
            若 （响应【「正文」】 是 空） 则
                置 响应【「正文」】 为 字节.从数列（【】）；
            否则
                若 非 （响应【「正文」】 是 字节串） 则
                    报错（「HTTP_BODY」，「言访响应正文必须是文字、字节串或空」）；
                终
            终
        终
        归 响应；
    终
    公 法 传输（）：法 则
        归 此.请求；
    终
    公 法 言访传输（） 则
        归 言访.自定义传输（此.发送言访，{}，「言试 HTTPMock」）；
    终
    公 法 各请求（）：列<典> 则
        归 复制列（此.请求列）；
    终
    公 法 请求次数（）：数 则
        归 长度（此.请求列）；
    终
    公 法 清空（）：HTTPMock 则
        置 此.请求列 为 【】；
        归 此；
    终
终

公 法 测试事务（连接，操作：法） 则
    定 事务 为 连接.开始事务（）；
    试 则
        定 值 为 操作（事务）；
        事务.回滚（）；
        归 值；
    救 所误 则
        试 则
            事务.回滚（）；
        救 回滚误 则
        终
        抛 所误；
    终
终

公 法 数据库测试事务（连接，操作：法） 则
    归 连接.托管事务（操作，言库.事务选项（）.完成后回滚（真））；
终

公 协 仓储协议 则
    法 重置（）：空；
    法 创建（数据：典）：典；
    法 读取（标识）：典?；
    法 更新（标识，变化：典）：典；
    法 删除（标识）：理；
    法 全部（）：列<典>；
终

公 类 函数仓储适配器 纳 仓储协议 则
    私 域 重置法：法；
    私 域 创建法：法；
    私 域 读取法：法；
    私 域 更新法：法；
    私 域 删除法：法；
    私 域 全部法：法；
    公 法 初始化（重置法：法，创建法：法，读取法：法，更新法：法，删除法：法，全部法：法） 则
        置 此.重置法 为 重置法；
        置 此.创建法 为 创建法；
        置 此.读取法 为 读取法；
        置 此.更新法 为 更新法；
        置 此.删除法 为 删除法；
        置 此.全部法 为 全部法；
    终
    公 法 重置（）：空 则
        此.重置法（）；
        归 空；
    终
    公 法 创建（数据：典）：典 则
        定 结果 为 此.创建法（复制典（数据））；
        若 非 （结果 是 典） 则
            报错（「REPOSITORY_ADAPTER」，「创建法必须返回典」）；
        终
        归 结果；
    终
    公 法 读取（标识）：典? 则
        定 结果 为 此.读取法（标识）；
        若 （（结果 是 空） 或 （结果 是 典）） 则
            归 结果；
        终
        报错（「REPOSITORY_ADAPTER」，「读取法必须返回典或空」）；
    终
    公 法 更新（标识，变化：典）：典 则
        定 结果 为 此.更新法（标识，复制典（变化））；
        若 非 （结果 是 典） 则
            报错（「REPOSITORY_ADAPTER」，「更新法必须返回典」）；
        终
        归 结果；
    终
    公 法 删除（标识）：理 则
        定 结果 为 此.删除法（标识）；
        若 非 （结果 是 理） 则
            报错（「REPOSITORY_ADAPTER」，「删除法必须返回理」）；
        终
        归 结果；
    终
    公 法 全部（）：列<典> 则
        定 结果 为 此.全部法（）；
        若 非 （结果 是 列） 则
            报错（「REPOSITORY_ADAPTER」，「全部法必须返回列」）；
        终
        逐 记录 于 结果 则
            若 非 （记录 是 典） 则
                报错（「REPOSITORY_ADAPTER」，「全部法的每项必须是典」）；
            终
        终
        归 结果；
    终
终

法 仓储契约要求（条件：理，阶段：文，消息：文）：空 则
    若 非 条件 则
        报错（「REPOSITORY_CONTRACT」，（（阶段 加 「：」） 加 消息））；
    终
    归 空；
终

法 仓储契约相同（实际，期望，阶段：文）：空 则
    若 （长度（深差异（实际，期望）） 大于 0） 则
        报错（「REPOSITORY_CONTRACT」，（阶段 加 「：仓储返回值不符合契约」））；
    终
    归 空；
终

公 法 验证仓储契约（仓储：仓储协议，初始：典，变化：典，主键：文）：典 则
    若 （（长度（主键） 小于 1） 或 （长度（主键） 大于 256）） 则
        报错（「REPOSITORY_INPUT」，「仓储主键须为一至 256 字符」）；
    终
    定 已验主键：文 为 主键；
    若 （（长度（初始） 小于 1） 或 （长度（初始） 大于 256）） 则
        报错（「REPOSITORY_INPUT」，「初始记录须含一至 256 个字段」）；
    终
    若 （（长度（变化） 小于 1） 或 （长度（变化） 大于 256）） 则
        报错（「REPOSITORY_INPUT」，「更新变化须含一至 256 个字段」）；
    终
    若 有所键（变化，已验主键） 则
        报错（「REPOSITORY_INPUT」，「更新变化不得修改主键」）；
    终
    令 主误：任意 为 空；
    令 清理误：任意 为 空；
    令 报告：典 为 {}；
    试 则
        仓储.重置（）；
        定 已创建：典 为 仓储.创建（复制典（初始））；
        仓储契约要求（有所键（已创建，已验主键），「创建」，「返回记录缺少主键」）；
        定 标识 为 已创建【已验主键】；
        仓储契约要求（（标识 不等于 空），「创建」，「主键不可为空」）；
        定 已读取：典? 为 仓储.读取（标识）；
        仓储契约要求（（已读取 是 典），「读取」，「已创建记录必须可读取」）；
        仓储契约相同（已读取，已创建，「读取」）；
        令 期望更新：典 为 复制典（已创建）；
        逐 键 于 变化 则
            置 期望更新【键】 为 复制值（变化【键】）；
        终
        定 已更新：典 为 仓储.更新（标识，复制典（变化））；
        仓储契约相同（已更新，期望更新，「更新」）；
        仓储契约相同（仓储.读取（标识），期望更新，「更新后读取」）；
        定 已列出：列<典> 为 仓储.全部（）；
        仓储契约要求（（长度（已列出） 等于 1），「列出」，「重置后的仓储应只含一项」）；
        仓储契约相同（已列出【0】，期望更新，「列出」）；
        仓储契约要求（仓储.删除（标识），「删除」，「首次删除必须返回真」）；
        仓储契约要求（（仓储.读取（标识） 是 空），「缺失读取」，「删除后读取必须返回空」）；
        仓储契约要求（非 仓储.删除（标识），「重复删除」，「重复删除必须返回假」）；
        仓储契约要求（（长度（仓储.全部（）） 等于 0），「删除后列出」，「删除后仓储必须为空」）；
        置 报告 为 {「成功」：真，「主键」：已验主键，「标识」：标识，「步骤」：【「创建」，「读取」，「更新」，「列出」，「删除」，「缺失读取」，「重复删除」】}；
    救 所误 则
        置 主误 为 所误；
    终
    试 则
        仓储.重置（）；
    救 所误 则
        置 清理误 为 所误；
    终
    若 （主误 是 误） 则
        抛 主误；
    终
    若 （清理误 是 误） 则
        报错（「REPOSITORY_CLEANUP」，「仓储契约验证后的重置失败」）；
    终
    归 报告；
终
