引「标准:JSON」为 JSON；

引「标准:数学」为 数学；

引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:文件」为 文件；

引「标准:路径」为 路径；

引「标准:环境」为 环境；

引「标准:标识」为 标识；

引「包:言时」为 言时；

公 定 版本：文 为 「0.1.0」；

令 临时序号：数 为 0；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANSHI_TEST_」 加 代码） 加 「：」） 加 消息）；
终

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 复制列（来源：列）：列 则
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，项）；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        置 结果【键】 为 来源【键】；
    终
    归 结果；
终

法 去键（来源：典，目标）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        若 （键 不等于 目标） 则
            置 结果【键】 为 来源【键】；
        终
    终
    归 结果；
终

法 整数文（值：数）：文 则
    归 文字.替换（JSON.序列化（值），「.0」，「」）；
终

法 文含（原文：文，片段：文）：理 则
    若 （长度（片段） 等于 0） 则
        归 真；
    终
    若 （长度（片段） 大于 长度（原文）） 则
        归 假；
    终
    令 位：数 为 0；
    当 （（位 加 长度（片段）） 不大于 长度（原文）） 则
        若 （原文【位：（位 加 长度（片段））】 等于 片段） 则
            归 真；
        终
        置 位 为 （位 加 1）；
    终
    归 假；
终

法 展示（值）：文 则
    试 则
        归 JSON.序列化（值）；
    救 所误 则
        归 （（「<」 加 类型（值）） 加 「>」）；
    终
终

法 错误典（所误：误）：典 则
    归 {「代码」：所误.代码，「类别」：所误.类别，「消息」：所误.消息，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

公 法 断言（条件：理，消息：文）：空 则
    若 非 条件 则
        报错（「ASSERT」，消息）；
    终
    归 空；
终

公 法 相等（实际，期望）：空 则
    若 （实际 不等于 期望） 则
        报错（「EQUAL」，（（（「期望 」 加 展示（期望）） 加 「，实际 」） 加 展示（实际）））；
    终
    归 空；
终

法 加差异（结果：列，路径值：文，代码：文，期望，实际）：空 则
    追加（结果，{「路径」：路径值，「代码」：代码，「期望」：期望，「实际」：实际}）；
    归 空；
终

公 法 深差异（实际，期望）：列 则
    令 结果：列 为 【】；
    法 比较（甲，乙，所径：文）：空 则
        若 （类型（甲） 不等于 类型（乙）） 则
            加差异（结果，所径，「TYPE」，类型（乙），类型（甲））；
            归 空；
        终
        若 （甲 是 列） 则
            若 （长度（甲） 不等于 长度（乙）） 则
                加差异（结果，所径，「LENGTH」，长度（乙），长度（甲））；
            终
            令 位：数 为 0；
            当 （（位 小于 长度（甲）） 且 （位 小于 长度（乙））） 则
                比较（甲【位】，乙【位】，（（（所径 加 「[」） 加 整数文（位）） 加 「]」））；
                置 位 为 （位 加 1）；
            终
            归 空；
        终
        若 （甲 是 典） 则
            逐 键 于 乙 则
                若 非 有所键（甲，键） 则
                    加差异（结果，（（所径 加 「.」） 加 展示（键）），「MISSING_KEY」，乙【键】，空）；
                否则
                    比较（甲【键】，乙【键】，（（所径 加 「.」） 加 展示（键）））；
                终
            终
            逐 键 于 甲 则
                若 非 有所键（乙，键） 则
                    加差异（结果，（（所径 加 「.」） 加 展示（键）），「EXTRA_KEY」，空，甲【键】）；
                终
            终
            归 空；
        终
        若 （甲 不等于 乙） 则
            加差异（结果，所径，「VALUE」，乙，甲）；
        终
        归 空；
    终
    比较（实际，期望，「$」）；
    归 结果；
终

公 法 深相等（实际，期望）：空 则
    定 差异：列 为 深差异（实际，期望）；
    若 （长度（差异） 大于 0） 则
        报错（「DEEP_EQUAL」，JSON.序列化（差异））；
    终
    归 空；
终

公 法 近似相等（实际：数，期望：数，容差：数）：空 则
    若 （容差 小于 0） 则
        报错（「TOLERANCE」，「容差不可为负数」）；
    终
    若 （数学.绝对值（（实际 减 期望）） 大于 容差） 则
        报错（「APPROXIMATE」，（（（（（「期望约等于 」 加 展示（期望）） 加 「，实际 」） 加 展示（实际）） 加 「，容差 」） 加 展示（容差）））；
    终
    归 空；
终

公 法 应含（容器，期望）：空 则
    令 已含：理 为 假；
    若 （容器 是 文） 则
        若 非 （期望 是 文） 则
            报错（「CONTAINS_TYPE」，「文字包含断言的期望值也须为文字」）；
        终
        置 已含 为 文含（容器，期望）；
    否则
        若 （容器 是 列） 则
            置 已含 为 包含（容器，期望）；
        否则
            若 （容器 是 典） 则
                置 已含 为 有所键（容器，期望）；
            否则
                报错（「CONTAINS_TYPE」，「包含断言只支持文、列或典」）；
            终
        终
    终
    若 非 已含 则
        报错（「CONTAINS」，（「容器不包含 」 加 展示（期望）））；
    终
    归 空；
终

公 法 应匹配（原文：文，模式：文）：空 则
    若 非 正则.匹配（模式，原文） 则
        报错（「MATCH」，（（（「文字不匹配模式 」 加 模式） 加 「：」） 加 原文））；
    终
    归 空；
终

公 法 类型断言（值，期望类型：文）：空 则
    定 实际类型：文 为 类型（值）；
    若 （实际类型 不等于 期望类型） 则
        报错（「TYPE」，（（（「期望类型 」 加 期望类型） 加 「，实际 」） 加 实际类型））；
    终
    归 空；
终

公 法 符合（值，判定：法，名称：文）：空 则
    若 非 判定（值） 则
        报错（「PREDICATE」，（「值不符合类型/条件 」 加 名称））；
    终
    归 空；
终

公 法 应当抛错（操作：法）：误 则
    试 则
        操作（）；
    救 所误 则
        归 所误；
    终
    报错（「THROWS」，「期望操作抛错，但操作成功」）；
终

公 法 应当抛错匹配（操作：法，模式：文）：误 则
    定 所误：误 为 应当抛错（操作）；
    若 非 正则.匹配（模式，所误.消息） 则
        报错（「THROWS_MATCH」，（（（「错误消息不匹配 」 加 模式） 加 「：」） 加 所误.消息））；
    终
    归 所误；
终

公 法 表格测试（名称：文，各行：列，测试法：法）：典 则
    令 通过数：数 为 0；
    令 结果：列 为 【】；
    令 位：数 为 0；
    逐 行 于 各行 则
        令 行名：文 为 （（名称 加 「 #」） 加 整数文（（位 加 1）））；
        若 （行 是 典） 则
            若 有所键（行，「名称」） 则
                置 行名 为 行【「名称」】；
            终
        终
        试 则
            测试法（行）；
            追加（结果，{「名称」：行名，「成功」：真}）；
            置 通过数 为 （通过数 加 1）；
        救 所误 则
            追加（结果，{「名称」：行名，「成功」：假，「错误」：错误典（所误）}）；
            报错（「TABLE」，（（行名 加 「：」） 加 所误.消息））；
        终
        置 位 为 （位 加 1）；
    终
    归 {「名称」：名称，「总数」：长度（各行），「通过」：通过数，「结果」：结果}；
终

公 法 参数化测试（名称：文，各参数：列<列>，测试法：法）：典 则
    令 各行：列 为 【】；
    令 位：数 为 0；
    逐 参数：列 于 各参数 则
        追加（各行，{「名称」：（（名称 加 「 #」） 加 整数文（（位 加 1））），「参数」：参数}）；
        置 位 为 （位 加 1）；
    终
    法 执行（行：典）：空 则
        测试法（行【「参数」】）；
        归 空；
    终
    归 表格测试（名称，各行，执行）；
终

公 类 测试夹具 则
    公 只 域 名称：文；
    私 域 工厂：法；
    私 域 清理法：法；
    私 域 已创建：理；
    私 域 值：任意；
    公 法 初始化（名称：文，工厂：法，清理法：法） 则
        置 此.名称 为 名称；
        置 此.工厂 为 工厂；
        置 此.清理法 为 清理法；
        置 此.已创建 为 假；
        置 此.值 为 空；
    终
    公 法 取得（） 则
        若 非 此.已创建 则
            置 此.值 为 此.工厂（）；
            置 此.已创建 为 真；
        终
        归 此.值；
    终
    公 法 清理（）：空 则
        若 此.已创建 则
            此.清理法（此.值）；
            置 此.值 为 空；
            置 此.已创建 为 假；
        终
        归 空；
    终
终

公 类 测试上下文 则
    私 域 夹具典：典；
    公 法 初始化（定义列：列） 则
        置 此.夹具典 为 {}；
        逐 定义：典 于 定义列 则
            定 所具 为 测试夹具（定义【「名称」】，定义【「工厂」】，定义【「清理」】）；
            置 此.夹具典【定义【「名称」】】 为 所具；
        终
    终
    公 法 夹具（名称：文） 则
        若 非 有所键（此.夹具典，名称） 则
            报错（「FIXTURE_UNKNOWN」，（「未知夹具 」 加 名称））；
        终
        归 此.夹具典【名称】.取得（）；
    终
    公 法 清理（）：空 则
        定 各名：列 为 键列（此.夹具典）；
        令 位：数 为 长度（各名）；
        当 （位 大于 0） 则
            置 位 为 （位 减 1）；
            此.夹具典【各名【位】】.清理（）；
        终
        归 空；
    终
终

公 类 测试套件 则
    公 只 域 名称：文；
    私 域 前全部：列<法>；
    私 域 后全部：列<法>；
    私 域 前每项：列<法>；
    私 域 后每项：列<法>；
    私 域 测试列：列<典>；
    私 域 夹具定义：列<典>；
    公 法 初始化（名称：文） 则
        置 此.名称 为 名称；
        置 此.前全部 为 【】；
        置 此.后全部 为 【】；
        置 此.前每项 为 【】；
        置 此.后每项 为 【】；
        置 此.测试列 为 【】；
        置 此.夹具定义 为 【】；
    终
    公 法 测试前（钩子：法）：测试套件 则
        追加（此.前全部，钩子）；
        归 此；
    终
    公 法 测试后（钩子：法）：测试套件 则
        追加（此.后全部，钩子）；
        归 此；
    终
    公 法 每项前（钩子：法）：测试套件 则
        追加（此.前每项，钩子）；
        归 此；
    终
    公 法 每项后（钩子：法）：测试套件 则
        追加（此.后每项，钩子）；
        归 此；
    终
    公 法 夹具（名称：文，工厂：法，清理法：法）：测试套件 则
        追加（此.夹具定义，{「名称」：名称，「工厂」：工厂，「清理」：清理法}）；
        归 此；
    终
    公 法 测试（名称：文，测试法：法）：测试套件 则
        追加（此.测试列，{「名称」：名称，「测试」：测试法}）；
        归 此；
    终
    公 法 运行（）：典 则
        令 结果：列 为 【】；
        令 失败数：数 为 0；
        逐 钩子：法 于 此.前全部 则
            钩子（）；
        终
        逐 所测：典 于 此.测试列 则
            定 上下文：测试上下文 为 测试上下文（此.夹具定义）；
            令 所错：任意 为 空；
            令 已有错误：理 为 假；
            试 则
                逐 钩子：法 于 此.前每项 则
                    钩子（上下文）；
                终
                所测【「测试」】（上下文）；
            救 错误 则
                置 所错 为 错误；
                置 已有错误 为 真；
            终
            试 则
                逐 钩子：法 于 此.后每项 则
                    钩子（上下文）；
                终
                上下文.清理（）；
            救 清理误 则
                若 非 已有错误 则
                    置 所错 为 清理误；
                    置 已有错误 为 真；
                终
            终
            若 （所错 是 误） 则
                追加（结果，{「名称」：所测【「名称」】，「成功」：假，「错误」：错误典（所错）}）；
                置 失败数 为 （失败数 加 1）；
            否则
                追加（结果，{「名称」：所测【「名称」】，「成功」：真}）；
            终
        终
        逐 钩子：法 于 此.后全部 则
            钩子（）；
        终
        若 （失败数 大于 0） 则
            报错（「SUITE」，（（此.名称 加 「 失败数：」） 加 整数文（失败数）））；
        终
        归 {「名称」：此.名称，「总数」：长度（此.测试列），「失败」：失败数，「结果」：结果}；
    终
终

公 法 套件（名称：文）：测试套件 则
    归 测试套件（名称）；
终

公 类 临时目录 则
    公 只 域 路径值：文；
    私 域 已清理：理；
    公 法 初始化（基础目录：文） 则
        若 非 文件.存在（基础目录） 则
            文件.创建目录（基础目录）；
        终
        置 临时序号 为 （临时序号 加 1）；
        定 种子：文 为 （（整数文（言时.系统时钟（）.毫秒（）） 加 「-」） 加 整数文（临时序号））；
        定 名称：文 为 （「yanxu-test-」 加 标识.稳定短码（种子，12））；
        置 此.路径值 为 路径.合并（基础目录，名称）；
        文件.创建目录（此.路径值）；
        置 此.已清理 为 假；
    终
    公 法 路径（）：文 则
        归 此.路径值；
    终
    公 法 合并（相对：文）：文 则
        归 路径.合并（此.路径值，相对）；
    终
    公 法 清理（）：空 则
        若 非 此.已清理 则
            若 文件.存在（此.路径值） 则
                文件.删除（此.路径值，真）；
            终
            置 此.已清理 为 真；
        终
        归 空；
    终
终

公 法 建临时目录（基础目录：文）：临时目录 则
    归 临时目录（基础目录）；
终

公 法 默认临时目录（）：临时目录 则
    归 临时目录（「.yanxu-test-tmp」）；
终

公 类 环境隔离 则
    私 域 基础：典；
    私 域 覆盖：典；
    私 域 已删：列<文>；
    公 法 初始化（基础：典） 则
        置 此.基础 为 复制典（基础）；
        置 此.覆盖 为 {}；
        置 此.已删 为 【】；
    终
    公 法 读取（名称：文）：文? 则
        若 包含（此.已删，名称） 则
            归 空；
        终
        若 有所键（此.覆盖，名称） 则
            归 此.覆盖【名称】；
        终
        若 有所键（此.基础，名称） 则
            归 此.基础【名称】；
        终
        归 空；
    终
    公 法 存在（名称：文）：理 则
        归 （此.读取（名称） 不等于 空）；
    终
    公 法 设置（名称：文，值：文）：环境隔离 则
        置 此.覆盖【名称】 为 值；
        若 包含（此.已删，名称） 则
            令 新列：列<文> 为 【】；
            逐 项：文 于 此.已删 则
                若 （项 不等于 名称） 则
                    追加（新列，项）；
                终
            终
            置 此.已删 为 新列；
        终
        归 此；
    终
    公 法 删除（名称：文）：环境隔离 则
        若 非 包含（此.已删，名称） 则
            追加（此.已删，名称）；
        终
        若 有所键（此.覆盖，名称） 则
            置 此.覆盖 为 去键（此.覆盖，名称）；
        终
        归 此；
    终
    公 法 转典（）：典 则
        令 结果：典 为 复制典（此.基础）；
        逐 名称：文 于 此.已删 则
            若 有所键（结果，名称） 则
                置 结果 为 去键（结果，名称）；
            终
        终
        逐 名称 于 此.覆盖 则
            置 结果【名称】 为 此.覆盖【名称】；
        终
        归 结果；
    终
    公 法 恢复（）：环境隔离 则
        置 此.覆盖 为 {}；
        置 此.已删 为 【】；
        归 此；
    终
终

公 法 隔离环境（基础：典）：环境隔离 则
    归 环境隔离（基础）；
终

公 法 隔离系统环境（名称列：列<文>）：环境隔离 则
    令 基础：典 为 {}；
    逐 名称：文 于 名称列 则
        若 环境.存在（名称） 则
            置 基础【名称】 为 环境.读取（名称）；
        终
    终
    归 环境隔离（基础）；
终

公 类 模拟函数 则
    私 域 实现法：法?；
    私 域 默认值：任意；
    私 域 返回列：列；
    私 域 抛错列：列<文>；
    私 域 调用列：列<典>；
    私 域 时钟：任意；
    公 法 初始化（时钟） 则
        置 此.实现法 为 空；
        置 此.默认值 为 空；
        置 此.返回列 为 【】；
        置 此.抛错列 为 【】；
        置 此.调用列 为 【】；
        置 此.时钟 为 时钟；
    终
    公 法 返回（值）：模拟函数 则
        置 此.默认值 为 值；
        归 此；
    终
    公 法 依次返回（各值：列）：模拟函数 则
        置 此.返回列 为 复制列（各值）；
        归 此；
    终
    公 法 依次抛错（各消息：列<文>）：模拟函数 则
        置 此.抛错列 为 复制列（各消息）；
        归 此；
    终
    公 法 实现（所法：法）：模拟函数 则
        置 此.实现法 为 所法；
        归 此；
    终
    公 法 调用（参数：列） 则
        定 序号：数 为 （长度（此.调用列） 加 1）；
        定 记录：典 为 {「序号」：序号，「参数」：复制列（参数），「时间戳」：此.时钟.毫秒（），「成功」：假}；
        若 （长度（此.抛错列） 大于 0） 则
            定 消息：文 为 此.抛错列【0】；
            置 此.抛错列 为 此.抛错列【1：】；
            置 记录【「错误」】 为 消息；
            追加（此.调用列，记录）；
            抛 消息；
        终
        试 则
            令 结果 为 此.默认值；
            若 （长度（此.返回列） 大于 0） 则
                置 结果 为 此.返回列【0】；
                置 此.返回列 为 此.返回列【1：】；
            否则
                若 （此.实现法 是 法） 则
                    置 结果 为 此.实现法（参数）；
                终
            终
            置 记录【「成功」】 为 真；
            置 记录【「结果」】 为 结果；
            追加（此.调用列，记录）；
            归 结果；
        救 所误 则
            置 记录【「错误」】 为 错误典（所误）；
            追加（此.调用列，记录）；
            抛 所误；
        终
    终
    公 法 函数（）：法 则
        归 此.调用；
    终
    公 法 调用次数（）：数 则
        归 长度（此.调用列）；
    终
    公 法 各调用（）：列<典> 则
        归 复制列（此.调用列）；
    终
    公 法 第次（序号：数）：典 则
        若 （（序号 小于 1） 或 （序号 大于 长度（此.调用列））） 则
            报错（「MOCK_CALL」，「调用序号越界」）；
        终
        归 此.调用列【（序号 减 1）】；
    终
    公 法 被调用于（参数：列）：理 则
        逐 记录：典 于 此.调用列 则
            若 （长度（深差异（记录【「参数」】，参数）） 等于 0） 则
                归 真；
            终
        终
        归 假；
    终
    公 法 清空（）：模拟函数 则
        置 此.调用列 为 【】；
        归 此；
    终
终

公 法 Mock（时钟）：模拟函数 则
    归 模拟函数（时钟）；
终

公 法 默认Mock（）：模拟函数 则
    归 模拟函数（言时.系统时钟（））；
终

公 类 Spy函数 则
    私 域 原函数：法；
    私 域 模拟：模拟函数；
    公 法 初始化（原函数：法，时钟） 则
        置 此.原函数 为 原函数；
        置 此.模拟 为 模拟函数（时钟）.实现（原函数）；
    终
    公 法 调用（参数：列） 则
        归 此.模拟.调用（参数）；
    终
    公 法 函数（）：法 则
        归 此.调用；
    终
    公 法 调用次数（）：数 则
        归 此.模拟.调用次数（）；
    终
    公 法 各调用（）：列<典> 则
        归 此.模拟.各调用（）；
    终
    公 法 第次（序号：数）：典 则
        归 此.模拟.第次（序号）；
    终
    公 法 被调用于（参数：列）：理 则
        归 此.模拟.被调用于（参数）；
    终
终

公 法 Spy（原函数：法，时钟）：Spy函数 则
    归 Spy函数（原函数，时钟）；
终

公 法 默认Spy（原函数：法）：Spy函数 则
    归 Spy函数（原函数，言时.系统时钟（））；
终

公 法 虚拟时钟（起始毫秒：数） 则
    归 言时.虚拟时钟（起始毫秒）；
终

公 类 快照库 则
    公 只 域 文件路径：文；
    私 域 更新：理；
    私 域 各快照：典；
    私 域 已改：理；
    公 法 初始化（文件路径：文，更新：理） 则
        置 此.文件路径 为 文件路径；
        置 此.更新 为 更新；
        置 此.各快照 为 {}；
        置 此.已改 为 假；
        若 文件.存在（文件路径） 则
            定 已读 为 JSON.解析（文件.读取（文件路径））；
            若 非 （已读 是 典） 则
                报错（「SNAPSHOT_FILE」，「快照文件根值必须是对象」）；
            终
            置 此.各快照 为 已读；
        终
    终
    公 法 匹配（名称：文，值）：空 则
        若 非 有所键（此.各快照，名称） 则
            若 非 此.更新 则
                报错（「SNAPSHOT_MISSING」，（「缺少快照 」 加 名称））；
            终
            置 此.各快照【名称】 为 值；
            置 此.已改 为 真；
            此.保存（）；
            归 空；
        终
        定 差异：列 为 深差异（值，此.各快照【名称】）；
        若 （长度（差异） 大于 0） 则
            若 此.更新 则
                置 此.各快照【名称】 为 值；
                置 此.已改 为 真；
                此.保存（）；
            否则
                报错（「SNAPSHOT_MISMATCH」，JSON.序列化（{「名称」：名称，「差异」：差异}））；
            终
        终
        归 空；
    终
    公 法 保存（）：空 则
        若 此.已改 则
            文件.写入（此.文件路径，JSON.序列化（此.各快照））；
            置 此.已改 为 假；
        终
        归 空；
    终
    公 法 名称列（）：列 则
        归 键列（此.各快照）；
    终
终

公 类 HTTPMock 则
    私 域 路由列：列<典>；
    私 域 请求列：列<典>；
    公 法 初始化（） 则
        置 此.路由列 为 【】；
        置 此.请求列 为 【】；
    终
    私 法 加路由（方法：文，匹配种类：文，地址：文，状态：数，首部：典，正文，剩余：数）：HTTPMock 则
        追加（此.路由列，{「方法」：文字.大写（方法），「匹配」：匹配种类，「地址」：地址，「状态」：状态，「首部」：复制典（首部），「正文」：正文，「剩余」：剩余}）；
        归 此；
    终
    公 法 响应（方法：文，地址：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「精确」，地址，状态，首部，正文，减 1）；
    终
    公 法 响应匹配（方法：文，模式：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「正则」，模式，状态，首部，正文，减 1）；
    终
    公 法 一次响应（方法：文，地址：文，状态：数，首部：典，正文）：HTTPMock 则
        归 此.加路由（方法，「精确」，地址，状态，首部，正文，1）；
    终
    公 法 响应JSON（方法：文，地址：文，状态：数，值）：HTTPMock 则
        归 此.响应（方法，地址，状态，{「content-type」：「application/json」}，JSON.序列化（值））；
    终
    公 法 请求（方法：文，地址：文，首部：典，正文） 则
        定 请求：典 为 {「方法」：文字.大写（方法），「地址」：地址，「首部」：复制典（首部），「正文」：正文}；
        追加（此.请求列，请求）；
        逐 路由：典 于 此.路由列 则
            令 地址合：理 为 假；
            若 （路由【「匹配」】 等于 「精确」） 则
                置 地址合 为 （地址 等于 路由【「地址」】）；
            否则
                置 地址合 为 正则.匹配（路由【「地址」】，地址）；
            终
            若 （（地址合 且 （请求【「方法」】 等于 路由【「方法」】）） 且 （路由【「剩余」】 不等于 0）） 则
                若 （路由【「剩余」】 大于 0） 则
                    置 路由【「剩余」】 为 （路由【「剩余」】 减 1）；
                终
                归 {「状态」：路由【「状态」】，「地址」：地址，「首部」：复制典（路由【「首部」】），「正文」：路由【「正文」】}；
            终
        终
        报错（「HTTP_UNMATCHED」，（（请求【「方法」】 加 「 」） 加 地址））；
    终
    公 法 传输（）：法 则
        归 此.请求；
    终
    公 法 各请求（）：列<典> 则
        归 复制列（此.请求列）；
    终
    公 法 请求次数（）：数 则
        归 长度（此.请求列）；
    终
    公 法 清空（）：HTTPMock 则
        置 此.请求列 为 【】；
        归 此；
    终
终

公 法 测试事务（连接，操作：法） 则
    定 事务 为 连接.开始事务（）；
    试 则
        定 值 为 操作（事务）；
        事务.回滚（）；
        归 值；
    救 所误 则
        试 则
            事务.回滚（）；
        救 回滚误 则
        终
        抛 所误；
    终
终
