引「../src/言试.yx」为 言试；

引「标准:测试」为 测试；

令 数据：典 为 {}；

令 下一编号：数 为 1；

令 重置次数：数 为 0；

法 有键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 重置仓储（）：空 则
    置 数据 为 {}；
    置 下一编号 为 1；
    置 重置次数 为 （重置次数 加 1）；
    归 空；
终

法 创建记录（输入：典）：典 则
    定 记录：典 为 言试.复制值（输入）；
    置 记录【「编号」】 为 下一编号；
    置 数据【下一编号】 为 言试.复制值（记录）；
    置 下一编号 为 （下一编号 加 1）；
    归 言试.复制值（记录）；
终

法 读取记录（编号）：典? 则
    若 非 有键（数据，编号） 则
        归 空；
    终
    归 言试.复制值（数据【编号】）；
终

法 更新记录（编号，变化：典）：典 则
    若 非 有键（数据，编号） 则
        抛 「测试仓储缺少记录」；
    终
    定 记录：典 为 言试.复制值（数据【编号】）；
    逐 键 于 变化 则
        置 记录【键】 为 言试.复制值（变化【键】）；
    终
    置 数据【编号】 为 言试.复制值（记录）；
    归 言试.复制值（记录）；
终

法 删除记录（编号）：理 则
    若 非 有键（数据，编号） 则
        归 假；
    终
    令 新数据：典 为 {}；
    逐 键 于 数据 则
        若 （键 不等于 编号） 则
            置 新数据【键】 为 数据【键】；
        终
    终
    置 数据 为 新数据；
    归 真；
终

法 全部记录（）：列<典> 则
    令 结果：列<典> 为 【】；
    逐 编号 于 数据 则
        追加（结果，言试.复制值（数据【编号】））；
    终
    归 结果；
终

定 仓储 为 言试.函数仓储适配器（重置仓储，创建记录，读取记录，更新记录，删除记录，全部记录）；

定 报告：典 为 言试.验证仓储契约（仓储，{「姓名」：「子衿」，「状态」：「待审」}，{「状态」：「启用」}，「编号」）；

测试.断言（报告【「成功」】，「完整 CRUD 契约应通过」）；

测试.相等（报告【「主键」】，「编号」）；

测试.相等（报告【「标识」】，1）；

测试.相等（长度（报告【「步骤」】），7）；

测试.相等（重置次数，2）；

测试.相等（长度（数据），0）；

令 坏重置次数：数 为 0；

法 坏重置（）：空 则
    置 坏重置次数 为 （坏重置次数 加 1）；
    归 空；
终

法 坏创建（输入：典）：典 则
    定 记录：典 为 言试.复制值（输入）；
    置 记录【「编号」】 为 1；
    归 记录；
终

法 坏读取（编号）：典? 则
    归 空；
终

法 坏更新（编号，变化：典）：典 则
    归 {}；
终

法 坏删除（编号）：理 则
    归 假；
终

法 坏全部（）：列<典> 则
    归 【】；
终

定 坏仓储 为 言试.函数仓储适配器（坏重置，坏创建，坏读取，坏更新，坏删除，坏全部）；

法 验证坏仓储（）：空 则
    言试.验证仓储契约（坏仓储，{「姓名」：「子衿」}，{「姓名」：「青青子衿」}，「编号」）；
    归 空；
终

言试.应当抛错代码（验证坏仓储，「YANSHI_TEST_REPOSITORY_CONTRACT」）；

测试.相等（坏重置次数，2）；

法 修改主键（）：空 则
    言试.验证仓储契约（仓储，{「姓名」：「子衿」}，{「编号」：2}，「编号」）；
    归 空；
终

言试.应当抛错代码（修改主键，「YANSHI_TEST_REPOSITORY_INPUT」）；

法 空主键（）：空 则
    言试.验证仓储契约（仓储，{「姓名」：「子衿」}，{「姓名」：「青青子衿」}，「」）；
    归 空；
终

言试.应当抛错代码（空主键，「YANSHI_TEST_REPOSITORY_INPUT」）；
