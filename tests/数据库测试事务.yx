引「../src/言试.yx」为 言试；

引「包:言库」为 言库；

引「标准:测试」为 测试；

令 请求列：列<典> 为 【】；

法 执行器（请求：典）：典 则
    追加（请求列，请求）；
    归 {「成功」：真，「列名」：【「值」】，「行」：【{「值」：1}】，「影响行数」：1}；
终

定 连接 为 言库.函数连接（「sqlite」，言库.SQLite方言（），执行器）；

令 已见回滚配置：理 为 假；

令 提交后次数：数 为 0；

法 不应提交（）：空 则
    置 提交后次数 为 （提交后次数 加 1）；
    归 空；
终

法 成功操作（事务）：文 则
    置 已见回滚配置 为 事务.配置（）【「完成后回滚」】；
    事务.执行（「UPDATE users SET active = ?」，【真】）；
    事务.提交后（不应提交）；
    归 「完成」；
终

测试.相等（言试.数据库测试事务（连接，成功操作），「完成」）；

测试.断言（已见回滚配置，「测试事务应显式启用完成后回滚」）；

测试.相等（提交后次数，0）；

测试.相等（长度（请求列），3）；

测试.相等（请求列【0】【「SQL」】，「BEGIN」）；

测试.相等（请求列【1】【「SQL」】，「UPDATE users SET active = ?」）；

测试.相等（请求列【2】【「SQL」】，「ROLLBACK」）；

置 请求列 为 【】；

法 失败操作（事务）：空 则
    事务.执行（「UPDATE users SET active = ?」，【假】）；
    言试.失败（「业务失败」）；
终

法 调用失败事务（）：空 则
    言试.数据库测试事务（连接，失败操作）；
    归 空；
终

言试.应当抛错代码（调用失败事务，「YANSHI_TEST_FAIL」）；

测试.相等（长度（请求列），3）；

测试.相等（请求列【2】【「SQL」】，「ROLLBACK」）；

置 请求列 为 【】；

测试.相等（言试.数据库测试事务（连接，成功操作），「完成」）；

测试.相等（请求列【0】【「SQL」】，「BEGIN」）；

测试.相等（请求列【（长度（请求列） 减 1）】【「SQL」】，「ROLLBACK」）；

连接.关闭（）；
