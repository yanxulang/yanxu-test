引「../src/言试.yx」为 言试；

引「标准:测试」为 测试；

定 时钟 为 言试.虚拟时钟（1000）；

定 模拟 为 言试.Mock（时钟）.依次返回（【「甲」，「乙」】）.返回（「末」）；

定 函数值：法 为 模拟.函数（）；

测试.相等（函数值（【1】），「甲」）；

时钟.推进（50）；

测试.相等（函数值（【2】），「乙」）；

测试.相等（函数值（【3】），「末」）；

测试.相等（模拟.调用次数（），3）；

测试.断言（模拟.被调用于（【2】），「应记录调用参数」）；

测试.相等（模拟.第次（2）【「时间戳」】，1050）；

法 相加（参数：列）：数 则
    归 （参数【0】 加 参数【1】）；
终

定 侦察 为 言试.Spy（相加，时钟）；

测试.相等（侦察.调用（【2，5】），7）；

测试.相等（侦察.调用次数（），1）；

测试.相等（侦察.第次（1）【「结果」】，7）；

定 会错Mock 为 言试.Mock（时钟）.依次抛错（【「模拟失败」】）；

法 调错（）：空 则
    会错Mock.调用（【】）；
    归 空；
终

言试.应当抛错匹配（调错，「模拟失败」）；

测试.相等（会错Mock.调用次数（），1）；
