引「../src/言试.yx」为 言试；

引「标准:测试」为 测试；

引「标准:文件」为 文件；

令 事件：列<文> 为 【】；

法 前全部（）：空 则
    追加（事件，「前全部」）；
    归 空；
终

法 后全部（）：空 则
    追加（事件，「后全部」）；
    归 空；
终

法 前每项（上下文）：空 则
    追加（事件，「前每项」）；
    归 空；
终

法 后每项（上下文）：空 则
    追加（事件，「后每项」）；
    归 空；
终

法 造夹具（）：文 则
    追加（事件，「造夹具」）；
    归 「资源」；
终

法 清夹具（值）：空 则
    追加（事件，（「清理」 加 值））；
    归 空；
终

法 测试体（上下文）：空 则
    言试.相等（上下文.夹具（「资源」），「资源」）；
    追加（事件，「测试」）；
    归 空；
终

定 所套 为 言试.套件（「生命周期」）.测试前（前全部）.测试后（后全部）.每项前（前每项）.每项后（后每项）.夹具（「资源」，造夹具，清夹具）.测试（「使用夹具」，测试体）；

测试.相等（所套.运行（）【「失败」】，0）；

言试.深相等（事件，【「前全部」，「前每项」，「造夹具」，「测试」，「后每项」，「清理资源」，「后全部」】）；

定 临时 为 言试.建临时目录（「tests/.tmp」）；

测试.断言（文件.存在（临时.路径（）），「临时目录应存在」）；

文件.写入（临时.合并（「数据.txt」），「内容」）；

临时.清理（）；

测试.断言（非 文件.存在（临时.路径（）），「临时目录应递归清理」）；

若 文件.存在（「tests/.tmp」） 则
    文件.删除（「tests/.tmp」，真）；
终

定 隔离 为 言试.隔离环境（{「模式」：「生产」，「端口」：「80」}）；

隔离.设置（「模式」，「测试」）.删除（「端口」）；

测试.相等（隔离.读取（「模式」），「测试」）；

测试.断言（非 隔离.存在（「端口」），「删除只影响隔离视图」）；

隔离.恢复（）；

测试.相等（隔离.读取（「模式」），「生产」）；
