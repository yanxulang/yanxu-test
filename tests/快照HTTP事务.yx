引「../src/言试.yx」为 言试；

引「标准:测试」为 测试；

引「标准:文件」为 文件；

定 临时 为 言试.建临时目录（「tests/.tmp-snapshot」）；

定 快照路径：文 为 临时.合并（「示例.snap.json」）；

定 更新库 为 言试.快照库（快照路径，真）；

更新库.匹配（「用户」，{「姓名」：「子衿」，「权限」：【「读」，「写」】}）；

定 验证库 为 言试.快照库（快照路径，假）；

验证库.匹配（「用户」，{「姓名」：「子衿」，「权限」：【「读」，「写」】}）；

法 坏快照（）：空 则
    验证库.匹配（「用户」，{「姓名」：「变化」}）；
    归 空；
终

言试.应当抛错匹配（坏快照，「SNAPSHOT_MISMATCH」）；

临时.清理（）；

若 文件.存在（「tests/.tmp-snapshot」） 则
    文件.删除（「tests/.tmp-snapshot」，真）；
终

定 HTTP 为 言试.HTTPMock（）.响应JSON（「GET」，「https://api.example.test/users/1」，200，{「id」：1}）.一次响应（「POST」，「https://api.example.test/users」，201，{}，「created」）；

定 响应 为 HTTP.请求（「GET」，「https://api.example.test/users/1」，{}，空）；

测试.相等（响应【「状态」】，200）；

测试.相等（HTTP.请求次数（），1）；

测试.相等（HTTP.请求（「POST」，「https://api.example.test/users」，{}，「{}」）【「状态」】，201）；

法 重复一次（）：空 则
    HTTP.请求（「POST」，「https://api.example.test/users」，{}，「{}」）；
    归 空；
终

言试.应当抛错匹配（重复一次，「HTTP_UNMATCHED」）；

令 已回滚：理 为 假；

公 类 假事务 则
    公 法 初始化（） 则
    终
    公 法 回滚（）：空 则
        置 已回滚 为 真；
        归 空；
    终
终

公 类 假连接 则
    公 法 初始化（） 则
    终
    公 法 开始事务（） 则
        归 假事务（）；
    终
终

法 事务体（事务）：文 则
    归 「结果」；
终

测试.相等（言试.测试事务（假连接（），事务体），「结果」）；

测试.断言（已回滚，「数据库测试事务必须回滚」）；
