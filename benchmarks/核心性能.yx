引「包:言试」为 言试；

引「标准:JSON」为 JSON；

引「标准:时间」为 时间；

引「标准:环境」为 环境；

引「标准:数学」为 数学；

定 参数列：列<文> 为 环境.参数（）；

若 （长度（参数列） 大于 1） 则
    抛 「YANSHI_BENCHMARK_ARGUMENT：只接受可选轮数」；
终

令 轮数：数 为 1000；

若 （长度（参数列） 等于 1） 则
    置 轮数 为 JSON.解析（参数列【0】）；
终

若 （（（数学.下取整（轮数） 不等于 轮数） 或 （轮数 小于 1）） 或 （轮数 大于 10000）） 则
    抛 「YANSHI_BENCHMARK_ROUNDS：轮数须为 1 至 10000 的整数」；
终

定 样本：典 为 {「用户」：{「编号」：42，「姓名」：「子衿」，「权限」：【「读」，「写」】}，「分页」：{「页」：3，「每页」：20}，「标记」：【真，假，空】}；

令 校验和：数 为 0；

令 次：数 为 0；

定 比较开始：数 为 时间.毫秒（）；

当 （次 小于 轮数） 则
    若 （长度（言试.深差异（样本，样本）） 不等于 0） 则
        抛 「YANSHI_BENCHMARK_COMPARE：相同样本出现差异」；
    终
    置 校验和 为 （校验和 加 1）；
    置 次 为 （次 加 1）；
终

定 比较毫秒：数 为 （时间.毫秒（） 减 比较开始）；

置 次 为 0；

定 复制开始：数 为 时间.毫秒（）；

当 （次 小于 轮数） 则
    定 副本：典 为 言试.复制值（样本）；
    若 （副本【「用户」】【「编号」】 不等于 42） 则
        抛 「YANSHI_BENCHMARK_COPY：复制结果不一致」；
    终
    置 校验和 为 （校验和 加 1）；
    置 次 为 （次 加 1）；
终

定 复制毫秒：数 为 （时间.毫秒（） 减 复制开始）；

定 模拟 为 言试.Mock（言试.虚拟时钟（0））.返回（1）；

置 次 为 0；

定 Mock开始：数 为 时间.毫秒（）；

当 （次 小于 轮数） 则
    置 校验和 为 （校验和 加 模拟.调用（【次】））；
    置 次 为 （次 加 1）；
终

定 Mock毫秒：数 为 （时间.毫秒（） 减 Mock开始）；

若 （模拟.调用次数（） 不等于 轮数） 则
    抛 「YANSHI_BENCHMARK_MOCK：Mock 调用记录数量不一致」；
终

若 （校验和 不等于 （轮数 乘 3）） 则
    抛 「YANSHI_BENCHMARK_CHECKSUM：基准校验和不一致」；
终

言 JSON.序列化（{「轮数」：轮数，「深比较毫秒」：比较毫秒，「深复制毫秒」：复制毫秒，「Mock毫秒」：Mock毫秒，「校验和」：校验和}）；
